mod main
//try import System Module by looking 
//for a folder 'System' in the current folder or in the stdlib dir
import System
//Explicit import System.nano
import "System.nano"
import "System.Console" as bla

class Program {
    let somevalue: int
    pub fnc Main(agrs: []string) {
        somevalue = 12
        println("Hello World");
        return 1
    }
    pub fnc someclass() {
        self.bla = 4
    }
}